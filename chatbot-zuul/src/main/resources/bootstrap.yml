server:
  port: 8060
spring:
  application:
    name: chatbot-zuul
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${eureka.server.port}/eureka/
#zuul:
#  routes:
#    chatbot-back: /chatbot-back/**
#    chatbot-auth-server: /uaa/**

ribbon:
  ReadTimeout: 10000
  SocketTimeout: 10000

